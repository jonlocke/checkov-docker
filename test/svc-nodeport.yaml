---
apiVersion: v1
kind: Service
metadata:
  name: checktest
  labels:
    app: checktest
spec:
  type: NodePort
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
    nodePort: 32100
  selector:
    app: checktest
